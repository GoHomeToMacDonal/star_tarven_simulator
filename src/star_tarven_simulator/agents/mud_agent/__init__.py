"""泥巴流（1 本理财三连）蒙特卡洛 Agent。

设计要点见本包 ``README`` 级别的模块 docstring：

* **启发式骨架**（:mod:`.heuristics` / :mod:`.policy`）：按泥巴流经验规则分阶段决策
  —— 抓理财卡（死神火车/蟑螂小队）、锁对子、给"合法升级池最小"的卡点黄金矿工、
  升本费降到 1/0 时再连升、8 回合 5 本 3 三连。
* **关键分叉前向采样**（:mod:`.mcts` / :mod:`.rollout`）：只在"发现选哪张 / 买哪张 /
  给谁点升级 / 是否升本"等关键分叉上，对每个候选动作 clone 局面并用骨架 rollout 到
  第 14 回合，取多次采样回报均值后 argmax。
* **分段回报**（:mod:`.reward`）：按"决策发生时的当前回合 k"分段——
  ``k < 6`` 只用 ``w3 * 三连数``（理论 6 回合内应连升到 3 本，此阶段只奖励凑三连）；
  ``k >= 6`` 用 ``w1 * R14 等效战力 + w2 * (14 - 达成5本回合) + w3 * 三连数``。

支持英雄：``陆战队员``（好兄弟，优先）、``工蜂``、``副官``。
"""

from star_tarven_simulator.agents.mud_agent.policy import MudAgent

__all__ = ["MudAgent"]
